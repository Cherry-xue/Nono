using BaseLib.Abstracts;
using MegaCrit.Sts2.Core.Combat;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;

namespace Nono.NonoCode.Singleton;

public class ScrollKeywords : CustomSingletonModel
{
    public ScrollKeywords() : base(HookType.Combat)
    {
    }

    public override async Task BeforeCardPlayed(CardPlay cardPlay)
    {
        var card = cardPlay.Card;

        // 如果是魔法卡，在打出前就标记为消耗
        if (card.Keywords.Contains(NonoKeywords.MagicCard))
        {
            card.ExhaustOnNextPlay = true;
        }
    }

    //public override async Task AfterCardPlayed(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    //{
    //    if (cardPlay.Card.Keywords.Contains(NonoKeywords.MagicCard))
    //    {
    //        var card = cardPlay.Card;

    //        // 防止重复操作
    //        if (card.HasBeenRemovedFromState || card.Pile?.Type == PileType.Exhaust)
    //        {
    //            return;
    //        }

    //        // 先移除（这会设置 HasBeenRemovedFromState = true）
    //        if (card.Pile != null && card.Pile.IsCombatPile)
    //        {
    //            await CardPileCmd.RemoveFromCombat(card);

    //            // ⚠️ 移除后 card.HasBeenRemovedFromState = true
    //            // 需要创建一个新的卡牌实例？或者重置状态？

    //            // 重置状态（因为我们要重新添加）
    //            card.HasBeenRemovedFromState = false;

    //            // 添加到 Exhaust
    //            await CardPileCmd.Add(card, PileType.Exhaust);
    //        }
    //    }
    //}
}
